# REST API with MongoDB

This is a simple REST API that allows you to perform CRUD operations on user data. It was built using Node.js, Express, and MongoDB with Mongoose. The API has four routes to get all users, add a new user, update a user by ID, and delete a user by ID. Environment variables are used to store the database connection string.
